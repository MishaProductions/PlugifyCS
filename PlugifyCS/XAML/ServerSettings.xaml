<Page x:Class="PlugifyCS.XAML.ServerSettings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PlugifyCS.XAML"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Server Settings"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Style="{StaticResource UiPage}">

    <TabControl>
        <TabItem Header="General settings">
            <Grid HorizontalAlignment="Left">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Button x:Name="btnDeleteServer" Foreground="Red" Grid.Row="0" Width="200" Click="btnDeleteServer_Click">Delete server</Button>
            </Grid>
        </TabItem>
        <TabItem Header="Invites"></TabItem>
    </TabControl>
</Page>
